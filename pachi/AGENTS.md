# Pachi Guidelines

## 1. Tech Stack & Environment
- **Engine**: Godot 4.7 (C# / Mobile target)
- **SDK & Runtime**: .NET 8.0 (`Godot.NET.Sdk/4.7.0`), Android build target `.NET 9.0`
- **Physics**: Rapier2D (`addons/godot-rapier2d`)
- **Solution**: `Pachi.sln` / `Pachi.csproj`

## 2. Structure & Conventions
- **Code Directory**: Source code organized by domain in `src/`
- **C# Scripts**: `PascalCase.cs` (e.g. [`PocketBallsIndicator.cs`](src/pockets/PocketBallsIndicator.cs))
- **Godot Scenes**: `snake_case.tscn` (e.g. `pocket.tscn`, `main_game.tscn`)
- **UID Sidecars**: Preserve all `.cs.uid` files generated by Godot's UID system
- **Path Portability**: Use project-relative paths exclusively for all file references, documentation, and agent instructions.

## 3. Build & Verification Commands
- **Unified Pipeline**: `./scripts/verify.sh` (Linux/macOS) or `.\scripts\verify.ps1` (Windows)
  - 3 verification stages:
    1. C# formatting & style (`dotnet format Pachi.sln --verify-no-changes`)
    2. Compiler & Roslyn analyzer verification (`dotnet build Pachi.sln` with `TreatWarningsAsErrors=true` and `AnalysisLevel=latest-recommended`)
    3. Headless Godot editor and test suite validation (`godot-mono --headless -s tests/TestRunner.cs`)
  - **Auto-Fix Formatting**: Run `./scripts/verify.sh --fix` (or `dotnet format Pachi.sln`) to automatically resolve formatting discrepancies.
- **Verification Gate**:
  - Run `./scripts/verify.sh` (or `.\scripts\verify.ps1` on Windows) after any C# or scene edit before concluding tasks.
  - Zero-warning bar: Verification must pass with 0 errors and 0 warnings.
  - Skip verification commands when no code or scene files changed.

## 4. Coding Standards & Invariants
- **Nullable Reference Types**: `<Nullable>enable</Nullable>` is enabled project-wide with zero warning tolerance (`TreatWarningsAsErrors=true`).
  - Declare optional references as nullable (`Type?`).
  - For required `[Export]` node references or properties initialized in `_Ready()`, declare as `Type?` and assert non-null in `_Ready()` (e.g. `Debug.Assert(NodeRef != null)`), or initialize with `= null!`.
- **Code Style & Formatting**: Enforced via `.editorconfig` (4 spaces, UTF-8, LF, Allman braces, PascalCase types/methods/properties, camelCase locals/parameters).
- **Partial Classes**: Godot C# scripts must be partial classes inheriting Godot nodes (e.g. `public partial class Pocket : Node2D`).
- **Attributes**:
  - `[GlobalClass]`: Custom node classes registered in Godot.
  - `[Tool]`: Editor-viewport scripts and custom `Resource` classes used by tool scripts.
  - `[Export]`: Inspector-exposed properties.
- **Redraw Property Setters**: In `[Tool]` scripts with `_Draw()`, use backing fields and call `QueueRedraw()` inside property setters. Reference: [`PocketBallsIndicator.cs`](src/pockets/PocketBallsIndicator.cs).
- **Node Validation**: Assert required `[Export]` node references in `_Ready()` using `Debug.Assert(...)`. Reference: [`Pocket.cs`](src/pockets/Pocket.cs).
- **Type-Safe API References**: Use source-generated `PropertyName` (e.g. `PropertyName.Scale`), `SignalName`, `nameof(...)`, or static `StringName` constants instead of string literals.
- **Lifecycle & Cleanup**: Unsubscribe (`-=`) from autoloads/singletons in `_ExitTree()`. For dynamic entities or single-use events, use `await ToSignal(...)` or `Connect(..., (uint)ConnectFlags.OneShot)`.
- **Async Signatures**: Return `Task` or `ValueTask` for all async methods.

## 5. Engine Configurations & Guardrails
- **Physics Layer 2**: Designated for `"Ball"`.
- **Input Action**: `launcher_charge` (mapped to Spacebar).
- **Language**: C# exclusively (use GDScript only when explicitly requested).

## Context Pointers
- **Coding Standards**: [`CODE_STYLE.md`](CODE_STYLE.md) — Consult when creating classes, defining signals/events, handling node lifecycle/cleanup, or resolving analyzer warnings.
- **Engine Reference**: `../ref/godot-docs/index.html` — Consult when verifying Godot 4.7 API contracts, node lifecycle behavior, or mobile export settings.

## Communication Guidelines

- The user is the primary coder. Act as a helpful consultant and executive assistant. Provide guidance, research, and help them stay organized. Do NOT write code or documentation without explicit instructions to do so. If user input appears to be asking you to write code or documentation, confirm using the `ask_question` tool before proceeding.

